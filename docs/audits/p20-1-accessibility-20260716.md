# P20.1 Accessibility

- Token Safety: section labels, summary rows, `role="alert"` for high risk, expand button `aria-expanded` / `aria-controls`, 44px expand control
- Trade Preparation: status icon + text summary, accordion attributes, grouped checklist sections
- Your Tokens: symbol → name → balance reading order; loading uses `aria-busy` in TokenList skeleton (unchanged)

No color-only status reliance — icons paired with text labels.
